import { Placeholder } from "@/components/Placeholder";

export default function PostJob() {
  return (
    <Placeholder
      emoji="📣"
      title="Post a job"
      body="Pick a role, set the pay range, and get a shareable link + QR — right from your phone. Wiring this up next."
    />
  );
}
